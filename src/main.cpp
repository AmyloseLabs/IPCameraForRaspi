#include <iostream>   // 표준 입출력 헤더

// 프로그램 시작점
int main(int argc, char* argv[]) {
    // argc : 실행 시 전달된 인자 개수
    // argv : 인자 값 배열 (argv[0]은 실행 파일 이름)

    std::cout << "Hello, Raspberry Pi 5!" << std::endl;

    // 프로그램 정상 종료
    return 0;
}